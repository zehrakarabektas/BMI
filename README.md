# BMI
This code is a simple user interface in Python using tkinter that calculates BMI (Body Mass Index). The interface prompts the user to enter their height and weight, calculates the BMI, and displays the result.
